#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
    int dice1, dice2, dice3 = 0;

    scanf("%d %d %d", &dice1, &dice2, &dice3);

    if (dice1 == dice2 && dice1 == dice3) printf("%d\n", 10000 * dice1);
    else (a == b || a == c || b == c)
        elif
}